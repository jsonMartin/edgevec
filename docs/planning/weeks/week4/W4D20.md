# EdgeVec Daily Task Guide — Day 20 (Week 4)

**Focus:** Browser E2E Testing & Demo
**Goal:** Verify everything works in a real browser environment.

---

## 1. Context
We have all pieces. Now we prove it works and fits the budget.

## 2. Tasks

### T20.1: E2E Test Suite
Create `tests/web.rs` using `wasm-bindgen-test`.
- Test Case 1: Basic Insert/Search loop.
- Test Case 2: Save/Load persistence loop.
- Test Case 3: Error handling (dimension mismatch).

### T20.2: Bundle Size Analysis
Run `wasm-pack build --release`.
- Check `.wasm` file size.
- If > 500KB, investigate `wee_alloc` or stripping debug symbols.
- **Target:** < 500KB (gzipped).

### T20.3: Demo Page (Optional/Stretch)
Create a simple HTML page in `examples/browser/` that:
1. Loads the WASM.
2. Generates random vectors.
3. Inserts them.
4. Performs a search.
5. Displays time taken.

## 3. Constraints
- **Headless Testing:** Must pass in CI environment (using `geckodriver` or `chromedriver`).

## 4. Verification
- All tests pass green.
- `ls -lh pkg/edgevec_bg.wasm` confirms size.

